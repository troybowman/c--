
.data

.text

_f0:

  # |--------------------------------|


__leave_f0:
  jr $ra

_f1:

  # |--------------------------------|
  # |           <padding>            |
  # |--------------------------------| sp+20
  # |              $ra               |
  # |--------------------------------| sp+16
  # |     <minimum 4 arg slots>      |
  # |--------------------------------| sp+0
  la $sp, -24($sp)
  sw $ra, 16($sp)

  jal _f0

__leave_f1:
  lw $ra, 16($sp)
  la $sp, 24($sp)
  jr $ra

_f2:

  # |--------------------------------|
  # |        <one is in $a0>         |
  # |--------------------------------| sp+0  <-- start of caller's frame


__leave_f2:
  jr $ra

_f3:

  # |--------------------------------|
  # |        <one is in $a0>         |
  # |--------------------------------| sp+24  <-- start of caller's frame
  # |           <padding>            |
  # |--------------------------------| sp+20
  # |              $ra               |
  # |--------------------------------| sp+16
  # |     <minimum 4 arg slots>      |
  # |--------------------------------| sp+0
  la $sp, -24($sp)
  sw $ra, 16($sp)

  jal _f0

__leave_f3:
  lw $ra, 16($sp)
  la $sp, 24($sp)
  jr $ra

_f4:

  # |--------------------------------|
  # |        <two is in $a1>         |
  # |--------------------------------| sp+4
  # |        <one is in $a0>         |
  # |--------------------------------| sp+0  <-- start of caller's frame


__leave_f4:
  jr $ra

_f5:

  # |--------------------------------|
  # |        <two is in $a1>         |
  # |--------------------------------| sp+28
  # |        <one is in $a0>         |
  # |--------------------------------| sp+24  <-- start of caller's frame
  # |           <padding>            |
  # |--------------------------------| sp+20
  # |              $ra               |
  # |--------------------------------| sp+16
  # |     <minimum 4 arg slots>      |
  # |--------------------------------| sp+0
  la $sp, -24($sp)
  sw $ra, 16($sp)

  jal _f0

__leave_f5:
  lw $ra, 16($sp)
  la $sp, 24($sp)
  jr $ra

_f7:

  # |--------------------------------|
  # |       <three is in $a2>        |
  # |--------------------------------| sp+8
  # |        <two is in $a1>         |
  # |--------------------------------| sp+4
  # |        <one is in $a0>         |
  # |--------------------------------| sp+0  <-- start of caller's frame


__leave_f7:
  jr $ra

_f8:

  # |--------------------------------|
  # |       <three is in $a2>        |
  # |--------------------------------| sp+32
  # |        <two is in $a1>         |
  # |--------------------------------| sp+28
  # |        <one is in $a0>         |
  # |--------------------------------| sp+24  <-- start of caller's frame
  # |           <padding>            |
  # |--------------------------------| sp+20
  # |              $ra               |
  # |--------------------------------| sp+16
  # |     <minimum 4 arg slots>      |
  # |--------------------------------| sp+0
  la $sp, -24($sp)
  sw $ra, 16($sp)

  jal _f0

__leave_f8:
  lw $ra, 16($sp)
  la $sp, 24($sp)
  jr $ra

_f9:

  # |--------------------------------|
  # |        <four is in $a3>        |
  # |--------------------------------| sp+12
  # |       <three is in $a2>        |
  # |--------------------------------| sp+8
  # |        <two is in $a1>         |
  # |--------------------------------| sp+4
  # |        <one is in $a0>         |
  # |--------------------------------| sp+0  <-- start of caller's frame


__leave_f9:
  jr $ra

_f10:

  # |--------------------------------|
  # |        <four is in $a3>        |
  # |--------------------------------| sp+36
  # |       <three is in $a2>        |
  # |--------------------------------| sp+32
  # |        <two is in $a1>         |
  # |--------------------------------| sp+28
  # |        <one is in $a0>         |
  # |--------------------------------| sp+24  <-- start of caller's frame
  # |           <padding>            |
  # |--------------------------------| sp+20
  # |              $ra               |
  # |--------------------------------| sp+16
  # |     <minimum 4 arg slots>      |
  # |--------------------------------| sp+0
  la $sp, -24($sp)
  sw $ra, 16($sp)

  jal _f0

__leave_f10:
  lw $ra, 16($sp)
  la $sp, 24($sp)
  jr $ra

_f11:

  # |--------------------------------|
  # |              five              |
  # |--------------------------------| sp+16
  # |        <four is in $a3>        |
  # |--------------------------------| sp+12
  # |       <three is in $a2>        |
  # |--------------------------------| sp+8
  # |        <two is in $a1>         |
  # |--------------------------------| sp+4
  # |        <one is in $a0>         |
  # |--------------------------------| sp+0  <-- start of caller's frame


__leave_f11:
  jr $ra

_f12:

  # |--------------------------------|
  # |              five              |
  # |--------------------------------| sp+40
  # |        <four is in $a3>        |
  # |--------------------------------| sp+36
  # |       <three is in $a2>        |
  # |--------------------------------| sp+32
  # |        <two is in $a1>         |
  # |--------------------------------| sp+28
  # |        <one is in $a0>         |
  # |--------------------------------| sp+24  <-- start of caller's frame
  # |           <padding>            |
  # |--------------------------------| sp+20
  # |              $ra               |
  # |--------------------------------| sp+16
  # |     <minimum 4 arg slots>      |
  # |--------------------------------| sp+0
  la $sp, -24($sp)
  sw $ra, 16($sp)

  jal _f0

__leave_f12:
  lw $ra, 16($sp)
  la $sp, 24($sp)
  jr $ra

_f14:

  # |--------------------------------|
  # |              six               |
  # |--------------------------------| sp+20
  # |              five              |
  # |--------------------------------| sp+16
  # |        <four is in $a3>        |
  # |--------------------------------| sp+12
  # |       <three is in $a2>        |
  # |--------------------------------| sp+8
  # |        <two is in $a1>         |
  # |--------------------------------| sp+4
  # |        <one is in $a0>         |
  # |--------------------------------| sp+0  <-- start of caller's frame


__leave_f14:
  jr $ra

_f15:

  # |--------------------------------|
  # |              six               |
  # |--------------------------------| sp+44
  # |              five              |
  # |--------------------------------| sp+40
  # |        <four is in $a3>        |
  # |--------------------------------| sp+36
  # |       <three is in $a2>        |
  # |--------------------------------| sp+32
  # |        <two is in $a1>         |
  # |--------------------------------| sp+28
  # |        <one is in $a0>         |
  # |--------------------------------| sp+24  <-- start of caller's frame
  # |           <padding>            |
  # |--------------------------------| sp+20
  # |              $ra               |
  # |--------------------------------| sp+16
  # |     <minimum 4 arg slots>      |
  # |--------------------------------| sp+0
  la $sp, -24($sp)
  sw $ra, 16($sp)

  jal _f0

__leave_f15:
  lw $ra, 16($sp)
  la $sp, 24($sp)
  jr $ra

_f16:

  # |--------------------------------|
  # |             seven              |
  # |--------------------------------| sp+24
  # |              six               |
  # |--------------------------------| sp+20
  # |              five              |
  # |--------------------------------| sp+16
  # |        <four is in $a3>        |
  # |--------------------------------| sp+12
  # |       <three is in $a2>        |
  # |--------------------------------| sp+8
  # |        <two is in $a1>         |
  # |--------------------------------| sp+4
  # |        <one is in $a0>         |
  # |--------------------------------| sp+0  <-- start of caller's frame


__leave_f16:
  jr $ra

_f17:

  # |--------------------------------|
  # |             seven              |
  # |--------------------------------| sp+48
  # |              six               |
  # |--------------------------------| sp+44
  # |              five              |
  # |--------------------------------| sp+40
  # |        <four is in $a3>        |
  # |--------------------------------| sp+36
  # |       <three is in $a2>        |
  # |--------------------------------| sp+32
  # |        <two is in $a1>         |
  # |--------------------------------| sp+28
  # |        <one is in $a0>         |
  # |--------------------------------| sp+24  <-- start of caller's frame
  # |           <padding>            |
  # |--------------------------------| sp+20
  # |              $ra               |
  # |--------------------------------| sp+16
  # |     <minimum 4 arg slots>      |
  # |--------------------------------| sp+0
  la $sp, -24($sp)
  sw $ra, 16($sp)

  jal _f0

__leave_f17:
  lw $ra, 16($sp)
  la $sp, 24($sp)
  jr $ra

main:

  # |--------------------------------|


__leavemain:
  jal __exit

__print_string:
  li $v0, 4
  syscall
  jr $ra

__print_int:
  li $v0, 1
  syscall
  jr $ra

__print_char:
  li $v0, 11
  syscall
  jr $ra

__print_hex:
  li $v0, 34
  syscall
  jr $ra

__exit:
  li $v0, 10
  syscall
  jr $ra
